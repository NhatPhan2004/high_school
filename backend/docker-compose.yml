version: '3.8'

networks:
    high_school:
        external: true

services:
    # postgres_db:
    #     container_name: postgresDB
    #     image: postgres:latest
    #     environment:
    #         - POSTGRES_USER=postgres
    #         - POSTGRES_PASSWORD=postgres
    #         - POSTGRES_DB=some-db
    #     restart: always
    #     ports:
    #         - '5436:5432'
    #     volumes:
    #         - ./db:/var/lib/postgresql/data
    # pgadmin:
    #     image: dpage/pgadmin4
    #     container_name: pgadmin4_container
    #     restart: always
    #     ports:
    #       - "5050:80"
    #     environment:
    #       PGADMIN_DEFAULT_EMAIL: raj@nola.com
    #       PGADMIN_DEFAULT_PASSWORD: admin
    #     volumes:
    #       - ./pgadmin-data:/var/lib/pgadmin
    # volumes:
    #     postgres_db:
    #         driver: local
    backend:
      container_name: backend
      build:
        context: .
        dockerfile: Dockerfile
      restart: always
      networks:
        - high_school
      command: yarn start:prod
      environment:
        # - PORT=6969
        - JWT_KEY_ACCESS=namnk
        - JWT_KEY_REFRESH=AKSJHKJSDF
        - EXPIRESIN=1000days
      ports:
        - "6868:8080"
      volumes:
        - /app

